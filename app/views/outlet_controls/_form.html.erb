<%= form_for(@outlet_control) do |f| %>
  <% if @outlet_control.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@outlet_control.errors.count, "error") %> prohibited this outlet_control from being saved:</h2>

      <ul>
      <% @outlet_control.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
