<%= form_for(@viewed) do |f| %>
  <% if @viewed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@viewed.errors.count, "error") %> prohibited this viewed from being saved:</h2>

      <ul>
      <% @viewed.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
